name: romm
services:
  romm:
    image: rommapp/romm:4.8.1@sha256:adb60df357a491f27eb8f2b40c6c255dd482867019ccd1ffe8eb6ca3db66d53e
    container_name: romm
    deploy:
      resources:
        reservations:
          memory: 512M
    networks:
      - romm-network
    restart: unless-stopped
    ports:
      - "8285:8080"
    environment:
      - DB_HOST=romm-db
      - DB_NAME=romm # Should match MARIADB_DATABASE in mariadb
      - DB_USER=romm-user # Should match MARIADB_USER in mariadb
      - DB_PASSWD=casaos # Should match MARIADB_PASSWORD in mariadb
      - ROMM_AUTH_SECRET_KEY="" # Generate a key with `openssl rand -hex 32`
      - IGDB_CLIENT_ID="" # Generate an ID and SECRET in IGDB
      - IGDB_CLIENT_SECRET="" # https://api-docs.igdb.com/#account-creation
      - SCREENSCRAPER_USER="" # Use your ScreenScraper username and password
      - SCREENSCRAPER_PASSWORD="" # https://docs.romm.app/latest/Getting-Started/Metadata-Providers/#screenscraper
      - STEAMGRIDDB_API_KEY="" # https://github.com/rommapp/romm/wiki/Metadata-Providers#steamgriddb
    volumes:
      - /DATA/AppData/$AppID/resources:/romm/resources # Resources fetched from IGDB (covers, screenshots, etc.)
      - /DATA/AppData/$AppID/redis-data:/redis-data # Cached data for background tasks
      - /DATA/AppData/$AppID/library:/romm/library # Your game library. Check https://github.com/rommapp/romm?tab=readme-ov-file#folder-structure for more details.
      - /DATA/AppData/$AppID/assets:/romm/assets # Uploaded saves, states, etc.
      - /DATA/AppData/$AppID/config:/romm/config # Path where config.yml is stored
    depends_on:
      romm-db:
        condition: service_healthy
        restart: true
  romm-db:
    image: mariadb:11.8.2@sha256:bb961ca672bcbde3df9e385d4ce84b31bb826792711c777f6ce97e49fcd9d38c
    container_name: romm-db
    restart: unless-stopped
    networks:
      - romm-network
    environment:
      - MARIADB_ROOT_PASSWORD=casaos # Use a unique, secure password
      - MARIADB_DATABASE=romm
      - MARIADB_USER=romm-user
      - MARIADB_PASSWORD=casaos
    volumes:
      - /DATA/AppData/$AppID/mysql:/var/lib/mysql
    healthcheck:
      test: [CMD, healthcheck.sh, --connect, --innodb_initialized]
      start_period: 30s
      start_interval: 10s
      interval: 10s
      timeout: 5s
      retries: 5
networks:
  romm-network:
    driver: bridge
x-casaos:
  id: org.icewhale.romm
  main: romm
  icon: https://cdn.jsdelivr.net/gh/IceWhaleTech/CasaOS-AppStore@gh-pages/apps/org.icewhale.romm/assets/icon.svg
  scheme: http
  port_map: "8285"
  index: /
  title:
    en_US: RomM
  version: "4.8.1"
