# 本次修复结果报告

生成时间：`2026-05-08`

工作分支：`add-field`

校验范围：`Apps/*/docker-compose.yml` 中 `x-casaos` 相关元数据，重点包括 `version`、`updateAt`、`releaseNotes`、`website`、`repo`、`support`、`docs`、`x-casaos.main`。

本次会话使用的处理标准：

- `releaseNotes` 优先以 GitHub Release / GitHub changelog / GitHub compare 为准。
- GitHub release body 为空时，不编造更新日志，明确说明该 tag 没有正文。
- `latest / main / master` 等浮动 tag 不伪装为可稳定映射的 release，统一说明需要 pin 到具体 tag。
- digest 镜像不自动推断单一 release，除非能可靠映射 digest 到 tag。
- `website / repo / support / docs` 只替换为可信官方来源，不用第三方索引页、同名无关项目或不可靠站点凑数。
- 对 GitHub-only 项目，如果没有可信官网或语义不确定，按反馈先将对应链接字段留空。

## 概览

- 原始待修复问题数：`421`
- 本次会话已处理的主要范围：
  - `missing_compose`
  - `missing_main_service`
  - `updateAt_mismatch`
  - `releaseNotes_placeholder`
  - `releaseNotes_mismatch`
  - `releaseNotes_version_mismatch`
  - `release_not_found`
  - `floating_tag_release_unverifiable`
  - `digest_release_unverifiable`
  - `broken_link`
  - 部分 `link_needs_review`
  - 部分 `website_semantics`
  - 部分 `website_equals_repo`
  - `support_equals_repo`
- 当前本地分支状态：`add-field` 相对 `origin/add-field` ahead `3`
- 当前工作树状态：本轮还有 `43` 个 compose 文件和两份结果产物未提交；未跟踪目录 `.casaos-gen/` 未纳入本轮修复产物。
- 最近一次 `git diff --check`：通过，仅有 Windows LF/CRLF warning

## 本次相关提交

- `50bb4d3 fix app metadata validation issues`
  - 早前已提交并 push。
  - 修复缺失 compose 文件、缺失 `x-casaos.main`、一批 broken links、87 个 `updateAt_mismatch`。
- `b8f1391 fix release notes metadata`
  - 本次会话提交。
  - 修复 release notes 元数据：141 个文件，335 insertions / 191 deletions。
- `0055d6b fix app links and websites`
  - 本次会话提交。
  - 修复 app links / websites：21 个文件，25 insertions / 25 deletions。
- `012994b fix taskingai website link`
  - 本次会话提交。
  - 修复 `TaskingAI` website broken link。

## 已修复问题统计

- `missing_compose`：`3 / 3`
- `missing_main_service`：`8 / 8`
- `updateAt_mismatch`：`87 / 87`
- `releaseNotes_placeholder`：`72 / 72`
- `releaseNotes_mismatch`：`44 / 44`
- `releaseNotes_version_mismatch`：`34 / 34`
- `release_not_found`：`37 / 37` 已复核并写入实际 changelog 或不可映射说明
- `floating_tag_release_unverifiable`：`21 / 21` 已复核并标注浮动 tag 风险
- `digest_release_unverifiable`：`1 / 1` 已复核并标注 digest 不可直接映射
- `broken_link`：`16 / 16`，当前复核结果 `bad_broken_links=0`
- `link_needs_review`：已按“不确定先留空”规范化；`Maybe` website 已置空。
- `support_equals_repo`：已修复；`Filedrop` 未确认源码来源，相关链接字段已置空。
- `website_semantics / website_equals_repo`：有可信官网的已修复；无可信官网或不确定的已置空。

## 关键修复明细

### critical

- `Jellyseerr`
  - `docker-compose.yaml` -> `docker-compose.yml`
- `Logseq`
  - `docker-compose.yaml` -> `docker-compose.yml`
- `Trilium`
  - `docker-compose.yaml` -> `docker-compose.yml`
- `AlbyHub`
  - 补齐 `x-casaos.main: albyhub`
- `EmulatorJS`
  - 补齐 `x-casaos.main: emulatorjs`
- `FlareSolverr`
  - 补齐 `x-casaos.main: flaresolverr`
- `Handbrake`
  - 补齐 `x-casaos.main: handbrake`
- `Netdata`
  - 补齐 `x-casaos.main: app`
- `RDTClient`
  - 补齐 `x-casaos.main: rdtclient`
- `Resilio-sync`
  - 补齐 `x-casaos.main: resilio-sync`
- `Snapdrop`
  - 补齐 `x-casaos.main: snapdrop`

### updateAt

- 按原始 CSV evidence 中的上游发布日期修复 `87` 个 `updateAt_mismatch`。
- 修复后曾运行校验脚本，结果为 `updateAt_bad=0`。

### releaseNotes

本轮重点按 GitHub changelog 标准处理 release notes：

- 有 GitHub Release / compare / 官方 changelog 的，写入具体变更点。
- 没有匹配 tag 或 release body 为空的，写入明确的不可映射说明。
- 浮动 tag 和 digest 不伪造 release。

代表性修复：

- `Radarr`
  - 写入 `5.26.2.10099` 的网络磁盘、metadata refresh、Discord notification、API docs 等变更。
- `Sonarr`
  - 写入 `4.0.15.2941` 的 Trakt authentication 与 IMDb ID 删除问题修复。
- `Prowlarr`
  - 写入 indexer sync 到 Radarr / Sonarr 的修复。
- `Pi-hole`
  - 写入 Docker `2025.11.1` 的 RISCV、FTL startup/log tailing 等变更。
- `Uptime Kuma`
  - 写入 `1.23.16` 安全依赖更新与 CVE-2024-56331 修复。
- `Deluge`
  - 写入 `2.2.0` 的安全修复、Python 支持变化、UI/Core 改动。
- `PhotoPrism`
  - 写入 build `250228` 的 Retina 5K thumbnails、WebDAV/M4V 等变更。
- `PostgreSQL`
  - 写入 `17.4` 的 libpq / CVE-2025-1094 相关修复。
- `CloudBeaver`、`OpenClaw`、`OpenSpeedTest`、`VoceChat`、`WireGuardEasy`
  - 标注当前 image tag 无法对应 GitHub Release。
- `LibreChat`、`RetroArch`、`StableDiffusionWebUI`、`Teable`、`Threadfin`、`Unifi-Network-Application`、`VirtualMachineManager`
  - 标注浮动 tag 需要 pin 到具体版本后才能稳定映射 changelog。

复核结果：

- release 相关 app 的 `releaseNotes` 块扫描结果：泛化占位句为 `0`

## 链接修复明细

### broken_link

当前重新访问原清单 `broken_link` 对应字段：`bad_broken_links=0`

- `CloudBeaver`
  - website -> `https://dbeaver.com/download/cloudbeaver/`
- `CopyParty`
  - website -> `https://copyparty.eu/`
- `Emby`
  - docs -> `https://support.emby.media/support/`
- `Emby_Nvidia`
  - docs -> `https://support.emby.media/support/`
- `HoloPlay`
  - website -> `https://github.com/stephane-r/HoloPlay`
  - docs -> `https://github.com/stephane-r/HoloPlay#readme`
- `JDownloader2`
  - repo -> `https://jdownloader.org/svn-changes`
  - 说明：源码实际走 SVN，但 `https://svn.jdownloader.org/` 返回 401；保留官方可访问 SVN changes 页面。
- `Komga`
  - docs -> `https://komga.org/docs/introduction/`
- `MariaDB`
  - support -> `https://mariadb.com/kb/en/getting-help-with-mariadb/`
- `MineOS`
  - website/docs -> `https://www.mineos.net/`
- `NetBird`
  - support -> `https://netbird.io/knowledge-hub/reporting-bugs-and-requesting-features-in-netbird`
- `Pingvin-Share`
  - website -> `https://stonith404.github.io/pingvin-share/`
- `Portainer`
  - support -> `https://docs.portainer.io/faqs/getting-support`
- `PyLoad`
  - docs -> `https://github.com/pyload/pyload/wiki`
- `TaskingAI`
  - website -> `https://docs.tasking.ai/`

### link_needs_review

已规范化的代表项：

- `ChatGPT-Next-Web`
  - website -> `https://nextchat.club`
  - docs -> `https://docs.nextchat.dev/introduction`
- `Dify`
  - docs -> `https://docs.dify.ai/en/use-dify/getting-started/introduction`
- `Duplicati`
  - website -> `https://duplicati.com/`
- `Gopeed`
  - docs -> `https://gopeed.com/docs`
- `Navidrome`
  - docs -> `https://www.navidrome.org/docs/`
- `OpenHands`
  - website -> `https://www.openhands.dev/`
  - docs -> `https://docs.all-hands.dev/`
- `playit-agent`
  - support -> `https://playit.gg/support/`
- `Tailscale`
  - docs -> `https://tailscale.com/docs`
- `Teable`
  - support/docs -> `https://help.teable.ai/en/basic/ai/overview`
- `Transmission`
  - website -> `https://transmissionbt.com/`
- `WireGuardEasy`
  - website -> `https://github.com/wg-easy/wg-easy`
  - docs -> `https://wg-easy.github.io/wg-easy/v15.1/`

置空项：

- `Maybe`
  - website -> `""`
  - `curl` 返回 403；虽然浏览器/网页抓取可打开，但按“所有不确定的先留空”口径处理。

## website / support 语义修复

已确认可信来源并修复：

- `DDNS-Updater`
  - website -> `https://hub.docker.com/r/qmcgaw/ddns-updater/`
- `Downtify`
  - website -> `https://downtify.henriquesebastiao.com`
- `Firefly`
  - website -> `https://qq.uusec.com/`
- `N8n`
  - website -> `https://n8n.io`
- `oPodSync`
  - website -> `https://fossil.kd2.org/opodsync/`
- `PdfDing`
  - website -> `https://www.pdfding.com`
- `Sure`
  - website -> `https://sure.am`
- `EmulatorJS`
  - website -> `https://emulatorjs.org/`
- `Handbrake`
  - website -> `https://handbrake.fr`
- `LyrionMusicServer`
  - support -> `https://forums.lyrion.org/`

## 遗留项

以下项目仍存在 `website=repo` 或 website 更像 GitHub 源码页的问题，但没有找到可信官方官网；未硬改：

- `Alist-Sync`
- `Calibre-web`
- `ConvertX`
- `Databag`
- `Ddns-go`
- `DeepSeek-OCR_Nvidia`
- `Embystat`
- `FlareSolverr`
- `Gateway-go`
- `Index-TTS`
- `Index-TTS_Nvidia`
- `Jackett`
- `Motioneye`
- `Mylar3`
- `Peanut`
- `Pinchflat`
- `PsiTransfer`
- `RDTClient`
- `StableDiffusionWebUI`
- `Threadfin`
- `TurboDiffusion_Nvidia`
- `WebDav`

保留原因：

- GitHub repo 的 `homepage` 字段为空，或不是适合作为官网的地址。
- 搜索结果多为第三方索引、同名无关项目或非官方页面。
- 对 GitHub-only 项目，如未找到可信官网，按反馈不再用 repo 兜底 website，而是留空。

### Filedrop

- image: `noecl/filedrop:1.0.1`
- 当前 Docker Hub 页面可访问：`https://hub.docker.com/r/noecl/filedrop`
- 未找到可信源码仓库。
- 同名 FileDrop 项目较多，不能直接替换。
- 建议人工确认镜像来源后，再决定 `repo`、`docs`、`support`、`website` 的最终字段。

## 验证记录

- `git diff --check`
  - 结果：通过
  - 说明：仅出现 Windows LF/CRLF warning
- `broken_link` 复核
  - 结果：`bad_broken_links=0`
- releaseNotes 占位语气扫描
  - 结果：release 相关块中泛化占位句为 `0`
- 当前 git 状态
  - `add-field...origin/add-field [ahead 3]`
  - 未跟踪目录：`.casaos-gen/`

## 建议下一步

- 提交并 push 当前 `add-field` 分支上的 3 个本地提交及本轮未提交修复。
- 对 `Filedrop` 做人工来源确认。
- 对仍为空的链接字段，后续仅在有可信官方来源时补回。

## 追加处理：不确定字段置空

根据后续反馈“所有不确定的先留空”，已追加处理：

- 对没有可信官网的 GitHub-only 项目，将 `website` 置空。
- 对 `DDNS-Updater`，GitHub homepage 指向 Docker Hub，但作为 `website` 语义不确定，已将 `website` 置空。
- 对 `Filedrop`，未确认 `noecl/filedrop:1.0.1` 的源码仓库、文档入口和支持入口，已将 `website`、`repo`、`support`、`docs` 全部置空。
- 对 `Maybe`，自动复核仍返回 403，已将 `website` 置空，保留确定的 GitHub `repo/support/docs`。

本轮置空涉及 `25` 个 app 文件：

- `Alist-Sync`
- `Calibre-web`
- `ConvertX`
- `Databag`
- `Ddns-go`
- `DDNS-Updater`
- `DeepSeek-OCR_Nvidia`
- `Embystat`
- `Filedrop`
- `FlareSolverr`
- `Gateway-go`
- `Index-TTS`
- `Index-TTS_Nvidia`
- `Jackett`
- `Maybe`
- `Motioneye`
- `Mylar3`
- `Peanut`
- `Pinchflat`
- `PsiTransfer`
- `RDTClient`
- `StableDiffusionWebUI`
- `Threadfin`
- `TurboDiffusion_Nvidia`
- `WebDav`

复核结果：

- `remaining_uncertain_links=0`
- `git diff --check` 通过，仅有 Windows LF/CRLF warning

## 追加处理：全量 compose 扫描

针对用户确认“所有 compose 是否都扫完”，已对当前仓库 `Apps/*/docker-compose.yml` 做全量扫描：

- app 目录数：`163`
- `docker-compose.yml` 文件数：`163`
- 缺失 `docker-compose.yml`：`0`
- 缺失 `x-casaos.main`：`0`
- 缺失 `version`：`0`
- 缺失 `updateAt`：`0`
- 缺失 `releaseNotes`：`0`
- 缺失 `website`：`0`
- 缺失 `repo`：`0`
- 缺失 `support`：`0`
- 缺失 `docs`：`0`
- `updateAt` 格式异常：`0`
- `releaseNotes` 占位/泛化语气：`0`
- 明显不确定链接：`0`
- URL 基本格式异常：`0`
- 非空 `website/repo/support/docs` 全量 GET 链接扫描：`612`
- `bad_links`：`0`

本轮全量扫描后新增修复：

- `Jellyseerr`
  - 补齐 `version/updateAt/releaseNotes/website/repo/support/docs`
  - 来源：GitHub Release `v2.7.3` 与 Seerr 官方站点。
- `Logseq`
  - 补齐 `version/updateAt/releaseNotes/website/repo/support/docs`
  - `0.10.10` tag 存在但无 GitHub Release 对象，使用 tag commit 日期，并在 releaseNotes 中明确说明不可映射 release body。
- `Trilium`
  - 补齐 `main`
  - 补齐 `version/updateAt/releaseNotes/website/repo/support/docs`
  - 来源：GitHub Release `v0.99.3` 与 TriliumNext 官方文档。
- `HoloPlay`
  - `website` 原为 GitHub repo，按不确定先留空。
- `WireGuardEasy`
  - `website` 原为 GitHub repo，按不确定先留空。
- `ActualBudget`
  - `support` 从 404 的 discussions 改为 GitHub issues。
- `Alist`
  - `website/docs` 从 `alist.nn.ci` 更新为最终规范域名 `alistgo.com`。
- `Crafty`
  - `support` 原 GitLab issues 返回 404，未确认替代入口，置空。
- `FileFlows`
  - `repo` 原 GitHub 地址返回 404，未确认源码仓库，置空。
- `JDownloader2`
  - `website/repo` 当前返回 503，置空；保留可访问的 support/docs。
- `Lazylibrarian`
  - `support` 从 GitLab issues 改为 GitHub issues。
- `LyrionMusicServer`
  - `support` 当前返回 403，置空。
- `Medusa`
  - `website` 当前返回 403/404，置空；保留 repo/support/docs。
- `OpenSpeedTest`
  - `website` 访问失败，置空。
  - `docs` 改为 GitHub README。
- `Twingate`
  - `repo` 当前返回 404，置空。

本轮验证：

- 全量字段完整性：通过
- 全量链接 GET 扫描：`checked_links=612`，`bad_links=0`
- `git diff --check`：通过，仅有 Windows LF/CRLF warning

## 最终全量扫描结论

已继续扫描到所有当前检查项完成：

- 本轮目标校验项已完成；未把仓库既有历史 metadata 形态差异作为本轮阻断项
- `bad_main_service_refs=0`
- `version_image_mismatch=0`
- `bad_updateAt_format=0`
- `release_notes_placeholder_like=0`
- `bad_url_format=0`
- `checked_links=612`
- `bad_links=0`

追加修复：

- `Blinko`
  - 合并 `upstream/main` 后按镜像 tag 为准，保留主镜像 `blinkospace/blinko:1.8.7`。
  - 将 `x-casaos.version/updateAt/releaseNotes` 对齐到 `1.8.7`。
- `Ddns-go`
  - 合并 `upstream/main` 后按镜像 tag 为准，主镜像为 `jeessy/ddns-go:v6.17.0`。
  - 将 `x-casaos.version/updateAt/releaseNotes` 对齐到 `v6.17.0`。
- `Downtify`
  - 合并 `upstream/main` 后按镜像 tag 为准，主镜像为 `ghcr.io/henriquesebastiao/downtify:2.6.0`。
  - 将 `x-casaos.version/updateAt/releaseNotes` 对齐到 `2.6.0`。- `HermesAgent`
  - 主镜像为 `latest`，将 `x-casaos.version` 改为 `latest`。
  - releaseNotes 改为浮动 tag 无法映射固定 changelog 的说明。
- `Linkwarden`
  - 合并 `upstream/main` 后按镜像 tag 为准，保留主镜像 `ghcr.io/linkwarden/linkwarden:v2.14.1`。
  - 将 `x-casaos.version/updateAt/releaseNotes` 对齐到 `v2.14.1`。
- `Logseq`
  - `x-casaos.version` 从 `0.10.10` 改为 `v0.10.10`，精确匹配镜像 tag。
- `Trilium`
  - `x-casaos.version` 从 `0.99.3` 改为 `v0.99.3`，精确匹配镜像 tag。

当前结论：所有 `Apps/*/docker-compose.yml` 已按本轮规则扫完；能确定的已修，不能确定的已留空。最新复核中 `version_image_mismatch=0`，即 `x-casaos.version` 已以主镜像 tag 为准。
